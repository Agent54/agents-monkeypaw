this is the monkeypaw opencode fork. you cannot touch files outside monkeypaw diretory without asking first and making sure merging upstream back is minimal overhead. we dont use anything from tui or other opencode packages except the opencode server and the opencode web app.

do not run any build commands ever.